# my-remote-usb-controller

provides two parts
* rest API that should run on raspberry pi 4 and controll the power on the usb port
* simple react app that calls the rest API and therefore constrolls power on the usb port 
